# Supabase

- **migrations/** — SQL migrations (e.g. `001_initial_schema.sql`). Run in Supabase SQL Editor or via Supabase CLI.

Database is used by `infrastructure/repositories/` in the Next.js app.
