# Database

**supabase.ts** — Supabase client used by the repository implementations in `../repositories/`.

Configure via env: `NEXT_PUBLIC_SUPABASE_URL`, `NEXT_PUBLIC_SUPABASE_ANON_KEY` (and service role if needed).
